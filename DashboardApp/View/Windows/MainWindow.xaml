<Window x:Class="DashboardApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DashboardApp"
        mc:Ignorable="d"
        Title="Дашборд" Height="610" Width="1280" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
        <!-- Меню -->
        <Border Background="#eee">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Button Name="DashboardBtn"
                        Content="Главная"
                        Click="DashboardBtn_Click"/>

                <Button Name="FridgesBtn" Grid.Column="2"
                        Content="Морозильные камеры" Click="FridgesBtn_Click">
                    
                </Button>
            </Grid>
        </Border>
        <!-- Страница навигации -->
        <Frame Name="MainFrame" Grid.Row="2"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
